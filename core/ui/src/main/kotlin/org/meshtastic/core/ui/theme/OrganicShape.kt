


package org.meshtastic.core.ui.theme

import androidx.compose.foundation.shape.CutCornerShape
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

val OrganicShapes = Shapes(
    extraSmall = RoundedCornerShape(0.dp),
    small = RoundedCornerShape(0.dp),
    medium = RoundedCornerShape(0.dp),
    large = RoundedCornerShape(0.dp),
    extraLarge = RoundedCornerShape(2.dp)
)

val RiverShape = RoundedCornerShape(0.dp)

val LeafShape = CutCornerShape(
    topStart = 8.dp,
    topEnd = 0.dp,
    bottomEnd = 8.dp,
    bottomStart = 0.dp
)

val PebbleShape = RoundedCornerShape(0.dp)

val SoftRectangleShape = RoundedCornerShape(0.dp)

val WaveShape = RoundedCornerShape(0.dp)

val HillShape = RoundedCornerShape(0.dp)
